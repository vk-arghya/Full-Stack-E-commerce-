# This root file is informational. Runtime env files live in frontend/.env and backend/.env.
# Do not place Firebase Admin private keys here.

# Frontend: see frontend/.env.example
# Backend: see backend/.env.example
