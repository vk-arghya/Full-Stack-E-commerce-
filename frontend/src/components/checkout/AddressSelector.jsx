import { CheckCircle2, Edit3, MapPin, Plus, Trash2 } from 'lucide-react';
export default function AddressSelector({addresses=[],selectedId,onSelect,onAdd,onEdit,onDelete}){return <div className="checkout-address-list">{addresses.map(a=>{const selected=selectedId===a.id;return <article key={a.id} className={`checkout-address-card ${selected?'checkout-address-selected':''}`}><button type="button" className="checkout-address-select" onClick={()=>onSelect?.(a)} aria-pressed={selected}><span className={`checkout-radio ${selected?'checkout-radio-selected':''}`}>{selected&&<span/>}</span><span className="min-w-0 flex-1 text-left"><span className="flex flex-wrap items-center gap-2"><b>{a.label||'Delivery address'}</b>{selected&&<span className="checkout-selected-label"><CheckCircle2 size={13}/> Selected</span>}</span><span className="mt-2 block font-bold">{a.name}</span><span className="mt-0.5 block text-sm text-stone-600">{a.phone}</span><span className="mt-2 block text-sm leading-6 text-stone-600">{[a.address,a.city,a.district,a.state].filter(Boolean).join(', ')}{a.pincode?` — ${a.pincode}`:''}</span></span></button><div className="checkout-address-actions"><button type="button" onClick={()=>onEdit?.(a)}><Edit3 size={14}/> Edit</button><button type="button" onClick={()=>onDelete?.(a)} className="text-red-700"><Trash2 size={14}/> Delete</button></div></article>})}{addresses.length<3&&<button type="button" onClick={onAdd} className="checkout-add-address"><Plus size={18}/> Add another address</button>}{!addresses.length&&<div className="empty-address"><MapPin size={26}/><div><b>No delivery address saved</b><p>Add your address to continue.</p></div></div>}</div>}
